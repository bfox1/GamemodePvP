package ml.gamemodepvp.Modules.core;

import ml.gamemodepvp.database.playerdata.CorePlayerData;

/**
 * Created by bfox1 on 5/17/2015.
 */
public class PermissionHelper
{


    public static String returnRankName(CorePlayerData data)
    {
    /*    String rank = "default";
        if(data.getRank().isDonor()) rank = "donor";
        else if(data.getRank().isAdmin()) rank = "Admin";
        else if(data.getRank().getGroupName()) rank = "Mod";

        return rank;
    }*/
        return "";
    }
}
